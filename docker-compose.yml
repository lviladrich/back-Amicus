
services:
  mysql:
    image: mysql:8.4
    container_name: amicus-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: amicus
      MYSQL_USER: amicus
      MYSQL_PASSWORD: amicus
    ports:
      - "3306:3306"
    volumes:
      - amicus-mysql-data:/var/lib/mysql

volumes:
  amicus-mysql-data:
